export enum PaymentStatus {
  PENDING = 'PENDING',
  PAID = 'PAID',
  CANCELED = 'CANCELED',
}

export enum PaymentProvider {
  CLICK = 'click',
  PAYME = 'payme',
  UZUM = 'uzum',
  PAYNET = 'paynet',
}
